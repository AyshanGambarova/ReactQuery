import { useQuery } from "react-query";
import axios from "axios";
const fetchData = () => {
  return axios.get(`https://dummyjson.com/quotes`);
};

export const useQuotesData = () => {
  return useQuery("quotes", fetchData, {});
};
